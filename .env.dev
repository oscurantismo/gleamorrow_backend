# Fake token for dev mode — won't affect production
BOT_TOKEN=development_dummy_token

# Only run Flask app (disable bot)
LOCAL_DEV=1

# Optional debug login
DEBUG_ADMIN_USER=admin
DEBUG_ADMIN_PASS=password

# Flask port (optional)
PORT=5000
